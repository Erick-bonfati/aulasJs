// Tipos de dados primitivos 

//String, number, undefined, null, boolean, symbol

let nomeAluno; // undefined = não aponta para lugar nenhun na memória

let sobrenomeAluno = null; // Nulo -> não aponta para local nenhum na memória

const aprovado = true; // valor booleano, true ou false (lógico)

let a = 2; 
const b = a;
console.log(a, b); // 2, 2

a = 3;
console.log(a, b); // 3, 2



