// Operadores aritméticos 

// ++ = incremento = faz a adição da variavel
// -- = decremento = faz a subtração da variavel

let contador = 1;
contador++;
console.log(contador++);
console.log(contador)

contador += 2;
contador += 2;
contador += 2;
console.log(contador);

// NaN - Not a number

// parseInt = converte o número para inteiro

// parseFloat = converte o número para float

// Number = converte para número seja inteiro ou float...

const num1 = 10;
const num2 = parseInt('5');
console.log(num1 + num2);