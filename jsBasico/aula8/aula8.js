// alert(''); = faz um alerta na página

// window.confirm = retorna um alerta com boolean ( true ou false )

// window.prompt = Cria uma caixa de seleção

alert('');

//prompt = é como se fosse um input do python