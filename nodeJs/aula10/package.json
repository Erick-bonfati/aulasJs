{
  "name": "aula10",
  "version": "1.0.0",
  "description": "Nesta aula vamos aprender a criar um servidor web com o node, que é um módulo que ele possui e podemos criar a partir dele, porém ele é bem complexo\r então vamos usar o \"express\" que deixa tudo na rota para a gente conseguir fazer.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.9",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
